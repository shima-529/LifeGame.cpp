class Cell {
	private:
		bool status;
		bool nextStatus;

	public:
		Cell();
		bool getStatus();
		bool getNextStatus();
		void setStatus(bool param);
		void setNextStatus(bool p);

		void goToNextStep();
		bool compareOwnStatus();
};
